@model IEnumerable<PrestationService.Models.Role>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.libRole)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.libRole)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idRole }) |
            @Html.ActionLink("Details", "Details", new { id=item.idRole }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idRole })
        </td>
    </tr>
}

</table>
