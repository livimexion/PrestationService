@model IEnumerable<PrestationService.Models.ChatRoom>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.libelleRoom)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.libelleRoom)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idRoom }) |
            @Html.ActionLink("Details", "Details", new { id=item.idRoom }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idRoom })
        </td>
    </tr>
}

</table>
