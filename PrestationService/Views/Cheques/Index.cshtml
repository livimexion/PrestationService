@model IEnumerable<PrestationService.Models.Cheque>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Paiement.typPay)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.numeroChk)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Paiement.typPay)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.numeroChk)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idCheque }) |
            @Html.ActionLink("Details", "Details", new { id=item.idCheque }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idCheque })
        </td>
    </tr>
}

</table>
