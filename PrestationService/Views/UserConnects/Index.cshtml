@model IEnumerable<PrestationService.Models.UserConnect>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ChatRoom.libelleRoom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Personne.nom)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ChatRoom.libelleRoom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Personne.nom)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idConnect }) |
            @Html.ActionLink("Details", "Details", new { id=item.idConnect }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idConnect })
        </td>
    </tr>
}

</table>
